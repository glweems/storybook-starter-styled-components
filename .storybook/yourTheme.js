import { create } from '@storybook/theming';

export default create({
  brandTitle: 'storybook starter react styled-components',
  brandUrl: 'https://github.com/glweems/storybook-starter-styled-components',
});
